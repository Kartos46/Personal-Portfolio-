# Core Requirements
Flask==2.0.1
Jinja2==3.0.1
Werkzeug==2.0.1

# Additional Dependencies (if used in the project)
python-dotenv==0.19.0  # For environment variables (if .env is used)
gunicorn==20.1.0       # For production deployment (optional)

# Development & Testing (optional)
pytest==6.2.4          # For testing (if tests are included)
Flask-Cors==3.0.10     # If CORS is needed for APIs
